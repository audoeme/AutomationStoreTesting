import { Page, Locator, expect } from '@playwright/test';

export class ShoppingCartPage {
    //readonly Page : Page;
    //readonly cart





    async validateCartCounter(expected: string) {
    //await expect(this.cartItemCount).toHaveText(expected);
  }


}